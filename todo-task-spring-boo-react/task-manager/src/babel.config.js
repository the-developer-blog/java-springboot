module.exports = {
    presets: [
      '@babel/preset-env', // Ensures compatibility with modern JS syntax
      '@babel/preset-react', // Ensures compatibility with JSX
      '@babel/preset-typescript', // Handles TypeScript files
    ],
  };
  